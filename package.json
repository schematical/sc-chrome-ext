{
  "name": "sc-monorepo",
  "private": true,
  "workspaces": [
    "packages/extension",
    "packages/server",
    "packages/langchain-a2a"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev:extension": "npm run dev --workspace sc-chrome-ext",
    "dev:server": "npm run dev --workspace sc-agent-server"
  }
}
